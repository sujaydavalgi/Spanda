{
  "name": "synapse",
  "version": "0.1.0",
  "description": "Synapse — a safe, strongly typed language for robot control and simulation",
  "type": "module",
  "bin": {
    "synapse": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "synapse": "node --import tsx src/cli/index.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
