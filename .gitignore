# node / build
node_modules/
dist/
target/

# env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# typescript
*.tsbuildinfo

# packages — build artifacts (source glue remains tracked)
packages/web/wasm/*.wasm
packages/web/wasm/spanda_wasm_bg.wasm.d.ts
packages/native/*.node
packages/native/native.js
packages/native/native.d.ts
packages/native/dist/
packages/native/index.js
packages/native/index.js.map
packages/native/index.d.ts

# logs / OS
*.log
.DS_Store

# local editor / git tooling — personal env only, not enforced repo-wide
# Optional .cursor/ agent rules and .githooks/ may live here locally; not tracked.
.cursor/
.githooks/

# test / coverage
coverage/

# cargo
.cargo/config.toml
crates/spanda-ros2-rclrs-native/Cargo.lock

# spanda runtime artifacts
.spanda-*.sd
.spanda/
*.trace

# python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
