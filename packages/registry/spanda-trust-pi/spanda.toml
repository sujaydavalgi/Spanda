[package]
name = "spanda-trust-pi"
version = "0.1.0"
description = "Raspberry Pi secure boot and EEPROM attestation contract stubs"
license = "Apache-2.0"

[safety]
level = "platform"

[capabilities]
required = [
  "trust.attest",
]

[hardware]
requirements = []
