[package]
name = "spanda-alert-teams"
version = "0.1.0"
description = "Microsoft Teams incoming-webhook alert channel for Spanda Control Center"
license = "Apache-2.0"

[safety]
level = "sensors"

[capabilities]
required = [
  "network.http",
]

[adapter]
provides = ["alerting.teams"]
requires = ["network.http"]
