# spanda-transport-websocket

WebSocket transport backend for Spanda. Extracted from `spanda-core` for the lean-core architecture.

Enable live broker integration with the `live` feature (`SPANDA_LIVE_WEBSOCKET=1` at runtime).

Used by official transport packages that negotiate WebSocket sessions.
