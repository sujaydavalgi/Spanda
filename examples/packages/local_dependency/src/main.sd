module fleet_app;

import std.robotics;
import std.sensors;

robot FleetBot {
    sensor lidar: Lidar on "/scan";
    actuator drive: DifferentialDrive;

    behavior patrol() {
        // Uses shared_utils types via dependency
    }
}
