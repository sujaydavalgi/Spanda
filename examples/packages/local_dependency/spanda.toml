[package]
name = "fleet_app"
version = "0.1.0"
description = "Fleet app with local dependency"
license = "Apache-2.0"

[dependencies]
shared_utils = { path = "shared_utils" }

[hardware]
targets = ["RoverV1", "JetsonOrin"]

[safety]
level = "experimental"
