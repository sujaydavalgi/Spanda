module warehouse_robot;

import std.robotics;
import std.sensors;
import navigation.path_planning;

robot WarehouseBot {
    sensor front_lidar: Lidar on "/scan";
    actuator drive: DifferentialDrive;

    behavior navigate() {
        // Basic warehouse navigation loop
    }
}
