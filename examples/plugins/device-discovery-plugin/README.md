# Device Discovery Plugin Example

Demonstrates device discovery integration with declared `device.read` / `device.write` capabilities.

```bash
spanda plugin install --path examples/plugins/device-discovery-plugin --approve-dangerous
```

Use `--approve-dangerous` when installing plugins that request write or network capabilities.
