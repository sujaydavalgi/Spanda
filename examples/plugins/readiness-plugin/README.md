# Readiness Plugin Example

Example **readiness** plugin demonstrating lifecycle hooks and capability declarations.

Install locally:

```bash
spanda plugin install --path examples/plugins/readiness-plugin
spanda plugin enable spanda-plugin-readiness-example
```

This plugin complements Spanda packages and providers — it does not replace them.
